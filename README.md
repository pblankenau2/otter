# otter
OTTER core

## Install OTTER

```
python -m build
python -m pip install dist/otter-1.0.0.tar.gz
```

Or equivalently: `./bi.sh`

## Getting data

`field_avg_extraction` contains scripts to pull data from EE.
`ee_soils_crop.py` requires `otter` to be installed.

## Running OTTER
